<%= form_with(model: @micropost, local: true) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.rich_text_area :content, placeholder: "Share new picture..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>